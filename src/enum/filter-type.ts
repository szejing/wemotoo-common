export enum FilterType {
	EQUAL = '=',
	NOT_EQUAL = '!=',
	IN = 'in',
	NOT_IN = 'not_in',
	LIKE = 'like',
	NOT_LIKE = 'not_like',
	GREATER_THAN = '>',
	LESS_THAN = '<',
	GREATER_THAN_OR_EQUAL = '>=',
	LESS_THAN_OR_EQUAL = '<=',
	BETWEEN = 'between',
}

// =, !=, in, not_in, like, not_like, >, <, >=, <=, between
